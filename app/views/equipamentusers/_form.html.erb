<br>
  <%= form_for(equipamentuser) do |f| %>
  
    <%= render 'shared/equipamentusers_error_messages' %>
    
    <%= f.select :equipament_id, Equipament.all.collect { |p| [ p.nome, p.id ] }, {prompt: 'Selecione o equipamento'}, {class: 'form-control'} %>
    
    <%= f.select :user_id, User.all.collect { |p| [ p.name, p.id ] }, {prompt: 'Selecione o usuário'}, {class: 'form-control'} %>
  
    <%= f.submit "Associar", class: "btn btn-primary" %>
    
  <% end %>
<br>
