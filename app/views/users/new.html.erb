<% provide(:title, 'Criar usuário') %>
<h1>Criar usuário</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user, url: signup_path) do |f| %>

      <%= render 'shared/error_messages' %>
      
      <%= f.label "Nome" %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label "Email" %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label "Tipo" %>
      <%= f.select(:usertype, [['Administrador', 'A'], ['Usuário', 'U']], {include_blank: true}, {class: 'form-control'}) %>
      
      <%= f.label "Crie uma senha" %>
      <%= f.password_field :password, class: 'form-control' %>

      <%= f.label "Confirme sua senha" , "Confirmation" %>
      <%= f.password_field :password_confirmation, class: 'form-control' %>

      <%= f.submit "Criar usuário", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

<br><br><br><br><br><br><br><br>